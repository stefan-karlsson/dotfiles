# Classify default and overlay applications

Type: grilling
Status: open
Blocked by: 01

## Question

Which existing managed packages, repositories, desktop applications, developer tools, shell integrations, and machine configuration belong to the always-on default profile versus the `private` and `company` overlay allowlists? Identify additions or removals needed for a coherent first version, while preserving the rule that applying an overlay never uninstalls an earlier overlay's applications.

## Comments
