# Verify official compatibility and installation channels

Type: research
Status: open

## Question

For Dash to Dock (`307`), Blur my Shell (`3193`), and AppIndicator Support (`615`), which official GNOME Extensions metadata, download paths, version constraints, and installation commands are valid for GNOME Shell 50.1 on Ubuntu 26.04? Define how chezmoi should select and refresh only compatible releases while failing clearly when an extension has no compatible version.
