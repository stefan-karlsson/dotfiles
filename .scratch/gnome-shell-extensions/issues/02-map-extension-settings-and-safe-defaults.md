# Map extension settings and safe Dracula-compatible defaults

Type: research
Status: open
Blocked by: 01

## Question

Where do each extension's enabled state and persistent preferences live, which schemas or command interfaces are stable enough to manage, and what minimum defaults support the existing Dracula desktop without overwriting user layout choices? Include the settings that must be left untouched and the behavior when an extension already has custom preferences.
