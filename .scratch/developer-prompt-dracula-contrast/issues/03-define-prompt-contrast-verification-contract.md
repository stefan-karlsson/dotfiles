# Define the Developer prompt contrast verification contract

Type: grilling
Status: open
Blocked by: 01, 02

## Question

What deterministic repository checks and manual Dracula Ptyxis smoke-check procedure are sufficient to verify the final Developer prompt color mapping? Define assertions against the rendered/source-managed `dot_p10k.zsh`, the state coverage required by the tests, and the visual scenarios a human must inspect so a future palette edit cannot silently reintroduce low contrast.

## Comments
