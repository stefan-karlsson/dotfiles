# Inventory Powerlevel10k rendering and color surface

Type: research
Status: open

## Question

Which Powerlevel10k settings and inline formatter escapes control the visible foreground, background, separators, and fallback behavior for every segment and state enabled by `home/dot_p10k.zsh` under the current lean/transparent style? Record the exact configuration surface that a Dracula contrast plan must cover, including directory variants, Git states, command duration, background jobs, Kubernetes context, clock, prompt glyph states, and custom Git formatting.

## Comments
