# 1Password SSH agent for GitHub authentication

GitHub SSH authentication uses the 1Password SSH agent. No private key is persisted under `~/.ssh`. Git SSH operations require the user's 1Password desktop account to be available and unlocked.
