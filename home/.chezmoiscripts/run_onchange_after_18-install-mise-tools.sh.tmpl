{{- if and (eq .chezmoi.os "linux") (eq .chezmoi.osRelease.id "ubuntu") -}}
#!/usr/bin/env bash

set -euo pipefail

command -v mise >/dev/null 2>&1 || {
  printf 'error: mise is unavailable after package installation\n' >&2
  exit 1
}

mise install
mise exec -- corepack enable
{{- end -}}
